# Gold checks

Place executable checks here after Gold integration is reviewed. Required areas: output grain, N:1 join cardinality, unmatched rows, aggregate reconciliation, and affected-month incremental behavior.
