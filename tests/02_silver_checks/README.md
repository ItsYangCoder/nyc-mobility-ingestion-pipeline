# Silver checks

Place executable checks here after the three Silver tables are reviewed. Required areas: row reconciliation, unique keys, nulls, dates, DST, duplicates, numeric flags, lineage, and rerun behavior.
