# Business-question SQL

Expected reviewed queries:

1. `01_highest_taxi_demand.sql`
2. `02_weather_and_trip_behavior.sql`
3. `03_zone_mobility_patterns.sql`

Add each query only with reproducible run instructions and a concise interpretation under `analytics/results/`.
