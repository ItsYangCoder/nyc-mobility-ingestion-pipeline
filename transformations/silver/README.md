# Silver transformations

Place reviewed source-specific Silver table definitions here:

- `green_taxi.py`
- `weather.py`
- `taxi_zones.py`

Implementations must follow `docs/architecture/data_model.md`, preserve source lineage, expose quality flags, and document input/output/rejected counts. Do not add placeholder table code.
